# Agent记忆管理：可组合性和缓存命中可以兼得

Agent 的记忆管理存在两个极端：**Composable（可组合性）**和 **Stable（缓存稳定性）**。现有的工作大都只能在两者之间二选一，Liana 试图用一套方法把两者粘合起来。

## 两个极端

|  | Composable | Stable |
|---|---|---|
| LLM 缓存命中 | 低 | 高 |
| 上下文稀疏性 | 高 | 低 |

**Composable（可组合）**代表灵活性：用户可以任意挑选需要的记忆片段，组合出一份高度聚焦的上下文。代价是这些记忆片段散落在各处，每次组合都需要"从头拼装"——缓存命中率很低，LLM 看到的上下文每次都不同。

**Stable（稳定）**代表性能：记忆之间存在固定的顺序关系（比如按时间线），上下文沿着这条固定路径一路追加即可，缓存命中率极高。代价是上下文会随着时间不断膨胀——无论是否相关，过去的东西全都跟着时间线一起涌进来，上下文越来越臃肿，信噪比越来越低。

任何记忆管理方法都位于这个光谱的某一点上，问题在于：**能不能在同一套机制里、针对同一次查询，动态地在这两个极端之间移动？**

## [Liana](https://github.com/windwhiterain/liana) 的方法

Liana 用一棵树把两者串起来。

| | Liana |
|---|---|
| LLM 缓存 | 记忆树，每个节点都是一段记忆 |
| 关联记忆 | 通过树节点上的约束来表达 |
| 稳定记忆顺序 | 节点的祖先链 |

核心思路是：**每一段对话被 LLM 压缩成一个 Memory（包含原始消息 + 摘要），逐段插入到一棵有向树中。每个树节点记录其所在子树的累积 token 数量。**

当用户需要召回若干段记忆（比如手动勾选、或让 LLM 自动筛选）时，Liana 在整棵树上搜索一个"最优挂载点"——找到那个能同时满足缓存命中高和上下文稀疏的树节点，然后把它的祖先链作为上下文喂给 LLM。

## Pipeline

整个过程分三步：

1. **筛选关联记忆。** 用户手动勾选，或让 LLM 根据当前问题从记忆列表中挑选相关的 index。
2. **搜索最优树节点。** 对树中的每个节点，计算将其作为上下文根节点的代价：
   - 找出关联记忆中**不在该节点祖先链里**的部分（missing memories）
   - 代价公式：`cost(node) = cache_hit_price * ancestor_chain(node).size + cache_miss_price * missing_memories(node).size`
   - 选代价最小的节点。
3. **取祖先链作为上下文。** 从最优节点向上遍历父节点，将沿途所有的 Memory（原始消息 + 摘要）拼接成 LLM 的 prompt 上下文。

## 代价函数如何实现两个极端

代价函数的关键是 `cache_hit_price` 和 `cache_miss_price` 的比值。

在 Liana 的实现里，`cache_hit_price = 1`，`cache_miss_price = 64`。这个 64 倍的惩罚意味着：**一段记忆如果不在当前树节点已有的缓存路径里，把它引入上下文的代价是其 token 量的 64 倍。**

- 如果把 `cache_miss_price` 调低（趋近 cache_hit_price），系统就偏向 **Composable**：它不再抗拒把散落的记忆拼到一起，代价很低，任意组合都行。但上下文每次都不一样，缓存命中率下降。
- 如果把 `cache_miss_price` 调高，系统就偏向 **Stable**：它极力避免引入不属于已有缓存路径的记忆，宁可用一个已经存在的大分支，哪怕里面夹杂了一些不相关的内容。上下文稳定，但稀疏性下降。

换言之，**这两个极端不再是非此即彼的设计选择，而是同一个代价函数在不同参数下的表现。** 那个 64 倍的参数是一个可以调节的旋钮，让你在一次查询里，动态地决定要偏"灵活"还是偏"稳定"。

## 为什么是一棵树而不是一个列表

如果顺序追加（列表），"稳定"很容易做到——每次把新的续在后面就行。但"可组合"做不到：散落在不同时间点的记忆没法被重新组织。

如果完全随机组合（集合），"可组合"很容易做到——任意挑选。但"稳定"做不到：每次组合出来的上下文都不一样，LLM 无法复用之前的计算。

树结构恰好卡在中间。**每个节点既有固定的祖先链（稳定顺序），又能接受来自任意方向的子节点挂载（可组合性）。** `find()` 做的事，就是在这棵树上找到一个点，使得"祖先链里已有的（缓存命中）"最大化，"需要从外面挂进来（缓存缺失）"最小化。

## 和现有工作的关系

- **MemGPT** 把上下文管理建模成操作系统的虚拟内存分页，通过 LLM function call 决定何时换入换出。分页机制保证了稳定性，但组合粒度粗——每次换入的是整段记忆，做不到精准稀疏。
- **HAT (Hierarchical Aggregate Tree)** 也用了树结构，但它的目标是用树遍历来最大化信息覆盖，没有显式建模缓存代价。
- **RAG / 向量检索**通过语义相似度检索记忆，组合性强，但检索出的 chunk 往往冗余且缺乏顺序信息，上下文稀疏但不稳定。

Liana 的不同之处在于：**它是缓存感知的。** 它不假定"哪些记忆最重要"（那是 LLM 筛选做的事），也不假定"按什么顺序放最好"（那是树结构天然提供的），它只做一件事——在记忆已经选定的前提下，找一个让上下文重建代价最小的组织方式。

## 实现

整个 memory system 大约 240 行 Rust，零外部依赖（除了 token 计数用的 `rig-memory`），不依赖向量数据库、embedding 模型或任何外部存储。所有记忆和树结构都驻留在进程内存中，服务于一个本地桌面 GUI 应用。

核心数据结构极其简洁：

- `Memory { messages, summary, size, nodes }` —— 一段对话 + LLM 摘要 + token 数量
- `Node { memory, parent, children, size }` —— 树节点，`size` 是子树的累积 token 数
- `Manager { memories, nodes, ... }` —— 管理所有记忆和树

两个关键方法：
- `add_memory(memory, parent)` —— 将记忆插入树中，继承父节点的累积 size
- `find(&[usize])` —— 给定一组记忆 index，返回最优树节点 + 两个诊断指标：
  - `theory_cache`：最终上下文中有多少是缓存命中的（越高越 Stable）
  - `memory_sparsity`：选中的上下文占总记忆的比例（越低越 Composable / 上下文越稀疏）

---

**总结**：Liana 提出了一种 Cache-aware Memory Composition 方法——用一棵代价感知的记忆树，把 Context Management 里相互矛盾的两个属性（可组合性和缓存稳定性）统一到同一个代价函数下。它的核心观点是：这两个极端不是设计缺陷，而是同一个优化问题的两种参数化表现。那个 64 倍的 cache_miss_price，就是在这两个极端之间滑动的旋钮。
